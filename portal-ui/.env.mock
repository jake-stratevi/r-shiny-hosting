# Loaded by `vite --mode mock` (npm run dev:mock).
# With this on, the API client never touches the network: every call is served
# from src/api/fixtures.ts. Nothing in the production build reads it.
VITE_PORTAL_MOCK=1
