# Runtime dependencies, pinned so an image rebuilt in six months is the image
# that was tested. Three, deliberately: aiohttp is both the server and the
# reverse-proxy client, boto3 is DynamoDB and ECS, PyYAML is only the seed
# tool's catalog reader.
#
# No aioboto3: boto3 calls go through asyncio.to_thread, which at this
# platform's traffic costs nothing and keeps one AWS SDK in the image.
aiohttp==3.14.3
boto3==1.43.79
PyYAML==6.0.3
